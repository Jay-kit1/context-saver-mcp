from __future__ import annotations


def local_search_stub(query: str) -> list[str]:
    return [f"Search query recorded: {query}", "Live search integration is planned for a later MCP/browser-backed version."]
